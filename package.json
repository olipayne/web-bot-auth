{
  "name": "web-bot-auth-repo",
  "description": "The monorepo for web-bot-auth",
  "type": "module",
  "keywords": [
    "web-bot-auth",
    "typescript"
  ],
  "author": "Thibault Meunier",
  "private": true,
  "scripts": {
    "build": "npm run build -w http-message-sig -w jsonwebkey-thumbprint -w web-bot-auth",
    "format": "prettier --write .",
    "lint": "eslint examples/**/*.ts packages/**/*.ts --ignore-pattern **/dist/** eslint.config.mjs",
    "test": "npm run test -w http-message-sig -- --run && npm run test -w jsonwebkey-thumbprint -- --run"
  },
  "license": "Apache-2.0",
  "version": "0.0.0",
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "eslint": "^9.21.0",
    "eslint-plugin-security": "^3.0.1",
    "prettier": "^3.5.3",
    "tsup": "^8.4.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.25.0",
    "vitest": "^3.0.7"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@11.3.0"
}
